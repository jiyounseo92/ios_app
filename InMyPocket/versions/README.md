# InMyPocket 버전 분리

- `v1_classic_web`: 기존(클래식) 웹 버전 스냅샷
- `v2_flex_web`: 새(Flex) 웹 버전 스냅샷

현재 실행용 원본은 기존 위치를 유지합니다.
- 클래식 실행 원본: `InMyPocket/` 루트의 web 파일
- Flex 실행 원본: `InMyPocket_Flex/`

필요하면 이 `versions` 아래 스냅샷을 기준으로 원하는 버전을 다시 배포할 수 있습니다.
